info:
  name: Update drivers costs
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/drivers/price"
  body:
    type: json
    data: |-
      {
        "acronyms": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
