info:
  name: Update driver pricing
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/admin/drivers/prices"
  body:
    type: json
    data: |-
      {
        "updateAllDrivers": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
