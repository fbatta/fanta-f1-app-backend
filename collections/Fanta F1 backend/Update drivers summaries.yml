info:
  name: Update drivers summaries
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/admin/drivers/summaries"
  body:
    type: json
    data: |-
      {
        "acronyms": [
          "VER"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
