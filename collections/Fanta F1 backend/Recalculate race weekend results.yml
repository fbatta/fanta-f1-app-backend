info:
  name: Recalculate race weekend results
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/race-weekends/recalculate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
